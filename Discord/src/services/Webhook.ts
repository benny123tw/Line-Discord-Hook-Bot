import axios from "axios";
import { MessageEmbed } from "discord.js";
import { Message } from "discord.js";
import { Bot } from "src/Bot";

export async function WebhookService(bot: Bot, message: Message) {
    
}